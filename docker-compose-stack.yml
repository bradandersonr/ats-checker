services:
  ats-web:
    image: bradandersonr/ats-checker:latest
    ports:
    - "5000:5000"
    deploy:
      replicas: 1
      restart_policy:
        condition: on-failure
      labels:
        - "traefik.enable=true"
        - "traefik.docker.network=traefik"  
        - "traefik.http.routers.ats.rule=Host(`ats.exampl.tech`)"
        - "traefik.http.routers.ats.entrypoints=websecure"
        - "traefik.http.routers.ats.tls=true"
        - "traefik.http.services.ats.loadbalancer.server.port=5000"
        - "traefik.http.routers.ats.middlewares=error-pages,forward-auth"
        - "traefik.constraint=proxy-public"
    networks:
      - traefik_webapp-dmz
